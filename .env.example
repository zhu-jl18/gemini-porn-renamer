# VideoRenamer 配置文件示例
# 复制此文件为 .env 并填写实际值

# ============================================
# LLM 后端配置
# ============================================

# 默认使用的 LLM 后端（gemini | openai）
LLM_BACKEND=gemini

# Gemini 后端配置（通过 GPT-Load 代理）
GEMINI_BASE_URL=http://localhost:3001/proxy/free
GEMINI_API_KEY=your-api-key-here
LLM_TRANSPORT=openai_compat
REQUEST_TIMEOUT=30
RETRY=3

# OpenAI 后端配置（可选）
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=your-openai-key
# OPENAI_ORGANIZATION=

# ============================================
# 模型配置
# ============================================

# 分析任务使用的模型（Flash 模型，速度快）
MODEL__FLASH=gemini-flash-latest

# 命名生成使用的模型（Pro 模型，质量高）
MODEL__PRO=gemini-2.5-pro

# ============================================
# 并发配置（两层并发）
# ============================================

# 第一层：子任务并发数（同时执行的分析任务数）
CONCURRENCY__TASK_CONCURRENCY=4

# 第二层：批次并发数（每个子任务内同时执行的批次数）
CONCURRENCY__BATCH_CONCURRENCY=16

# ============================================
# 分析配置
# ============================================

# 分析任务配置文件路径
ANALYSIS__TASKS_CONFIG_PATH=config/analysis_tasks.yaml

# 分析提示词目录
ANALYSIS__PROMPTS_DIR=config/prompts/analysis

# 每批次的帧数（Gemini 限制为 5）
ANALYSIS__BATCH_SIZE=5

# ============================================
# 命名配置
# ============================================

# 默认使用的命名风格（逗号分隔）
NAMING__STYLES=chinese_descriptive,scene_role,pornhub_style,concise

# 命名风格配置文件路径
NAMING__STYLE_CONFIG_PATH=config/naming_styles.yaml

# 命名提示词目录
NAMING__PROMPTS_DIR=config/prompts/naming

# 每个风格生成的候选数
NAMING__CANDIDATES_PER_STYLE=1

# 总候选数上限
NAMING__TOTAL_CANDIDATES=5

# ============================================
# 日志配置
# ============================================

# 日志目录
LOG_DIR=logs

# 日志级别（DEBUG | INFO | WARNING | ERROR | CRITICAL）
LOG_LEVEL=INFO

# ============================================
# 说明
# ============================================

# 1. 环境变量使用双下划线 __ 表示嵌套层级
#    例如：CONCURRENCY__TASK_CONCURRENCY 对应 config.concurrency.task_concurrency
#
# 2. GPT-Load 配置：
#    - 确保 GPT-Load 服务运行在 http://localhost:3001
#    - API Key 从 GPT-Load 获取
#    - 支持上万个 Gemini key 自动轮询
#
# 3. 并发策略：
#    - 第一层并发：4 个子任务（角色、脸部、场景、姿势）同时执行
#    - 第二层并发：每个子任务内 16 个批次同时调用 API
#    - 总并发数：4 × 16 = 64 次 API 调用
#
# 4. 命名风格：
#    - chinese_descriptive: 中文描述性（温泉旅馆的诱惑）
#    - scene_role: 场景+角色（温泉旅馆_美丽人妻）
#    - pornhub_style: P站风格（Hot MILF seduced in hotel）
#    - concise: 简洁标题（温泉诱惑）
